{
  "version": "v1",
  "hooks": [
    {
      "name": "Test on TypeScript save",
      "trigger": "PostFileSave",
      "description": "Runs the deterministic test suite after TypeScript source files are saved.",
      "matcher": "src/.*\\.ts$",
      "action": {
        "type": "command",
        "command": "npm test",
        "timeout": 120
      }
    }
  ]
}